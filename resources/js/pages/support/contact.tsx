import { Head, useForm } from '@inertiajs/react';
import AppLayout from '../../layouts/app-layout';
import { LifeBuoy } from 'lucide-react';

export default function Contact({requests,canManage}:any){const f=useForm({subject:'',category:'general',message:'',priority:'normal'});return <AppLayout title="Contact Us / Support"><Head title="Support"/><div className="grid gap-5 xl:grid-cols-[380px_1fr]"><form className="hf-card p-5 space-y-3" onSubmit={e=>{e.preventDefault();f.post('/support/contact',{onSuccess:()=>f.reset('subject','message')})}}><div className="flex items-center gap-2"><LifeBuoy size={18}/><h2 className="font-extrabold">New support request</h2></div><Field label="Subject"><input className="hf-input" value={f.data.subject} onChange={e=>f.setData('subject',e.target.value)}/></Field><div className="grid grid-cols-2 gap-3"><Field label="Category"><select className="hf-input" value={f.data.category} onChange={e=>f.setData('category',e.target.value)}>{['general','access','data','technical','content','other'].map(x=><option key={x} value={x}>{x}</option>)}</select></Field><Field label="Priority"><select className="hf-input" value={f.data.priority} onChange={e=>f.setData('priority',e.target.value)}><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option></select></Field></div><Field label="Message"><textarea className="hf-input min-h-36" value={f.data.message} onChange={e=>f.setData('message',e.target.value)}/></Field><button className="hf-btn">Submit Request</button></form><section className="space-y-4">{requests.map((r:any)=><SupportCard key={r.id} r={r} canManage={canManage}/>)}</section></div></AppLayout>}
function SupportCard({r,canManage}:any){const f=useForm({status:r.status,response_note:r.response_note??''});return <article className="hf-card p-5"><div className="flex flex-wrap justify-between gap-3"><div><div className="text-xs font-semibold text-[#78677e]">#{r.id} · {r.category} · {r.center?.code??'Global'} · {r.user?.name??'User'}</div><h2 className="mt-1 font-extrabold">{r.subject}</h2></div><span className="hf-badge">{r.status}</span></div><p className="mt-3 whitespace-pre-wrap text-sm leading-6">{r.message}</p>{r.response_note&&<div className="mt-3 rounded-xl bg-[#f8f1fa] p-3 text-sm"><b>Response:</b> {r.response_note}</div>}{canManage&&<form className="mt-4 grid gap-2 sm:grid-cols-[160px_1fr_auto]" onSubmit={e=>{e.preventDefault();f.put(`/support/contact/${r.id}`)}}><select className="hf-input" value={f.data.status} onChange={e=>f.setData('status',e.target.value)}><option value="open">Open</option><option value="in_progress">In Progress</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select><input className="hf-input" placeholder="Response note" value={f.data.response_note} onChange={e=>f.setData('response_note',e.target.value)}/><button className="hf-btn">Update</button></form>}</article>}
function Field({label,children}:{label:string;children:any}){return <div><label className="hf-label">{label}</label>{children}</div>}
